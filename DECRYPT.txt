SELECT TEM.EMPLOYEE_CODE,TEM.EMPLOYEE_NAME,TEM.LOGIN_NAME,F_MANAGE_PASS('Decrypt',LOGIN_PWD,'SEA.NET.') as PWD,TLM.LOCATION_NAME
FROM T_AUTH_EMPLOYEE_MASTER TEM, T_APP_LOCATION_MASTER TLM
WHERE TEM.LOCATION_ID = TLM.LOCATION_ID

 